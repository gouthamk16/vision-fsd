# ByteTrack settings tuned for vehicle tracking on forward-driving video.
# Lower thresholds let the tracker keep IDs through short confidence dips.

tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.05
new_track_thresh: 0.30
track_buffer: 60
match_thresh: 0.85
fuse_score: True
